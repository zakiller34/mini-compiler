# tests/CMakeLists.txt — filled per phase
# Unit tests (Google Test)
# add_subdirectory(unit)

# Z3 predicate tests
# if(Z3_FOUND)
#     add_subdirectory(z3)
# endif()

# Integration tests
# add_subdirectory(integration)
